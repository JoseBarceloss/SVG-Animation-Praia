import { Cards } from './card.js';

// Função que chama a animação dos cards
window.onload = () => {
    let animationCard = new Cards();
    document.addEventListener('scroll', animationCard.
        scrollcards.bind(animationCard));
};